[build-system]
requires = ["setuptools>=42"]
build-backend = "setuptools.build_meta"

[project]
name = "roozerball"
version = "0.1.0"
description = "Roozerball game engine and Godot bridge"
requires-python = ">=3.8"

[tool.setuptools.packages.find]
where = ["."]
include = ["roozerball*"]
